# ===========================================
# SIG Chatbot Environment Variables
# ===========================================

# Server Configuration
PORT=3000
NODE_ENV=development

# ===========================================
# API KEYS (Required)
# ===========================================
OPENAI_API_KEY=sk-your-openai-key-here

# Optional - Only needed if using Gemini models
GOOGLE_AI_API_KEY=

# ===========================================
# DATABASE (Required for Production)
# ===========================================
# Supabase or any PostgreSQL connection string
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres

# ===========================================
# REDIS (Required for Production)
# ===========================================
# Upstash, Render Redis, or any Redis URL
REDIS_URL=redis://default:xxxxx@xxx.upstash.io:6379

# ===========================================
# WEBHOOK SECURITY
# ===========================================
# Set a secret for ManyChat webhook verification
WEBHOOK_SECRET=your-secure-webhook-secret

# ===========================================
# MODEL CONFIGURATION (Optional - Defaults shown)
# ===========================================
# Responder Model - Used for chat responses
RESPONDER_MODEL=gpt-4o-mini
RESPONDER_MAX_TOKENS=1500
RESPONDER_TEMPERATURE=0.7

# Extractor Model - Used for data extraction
EXTRACTOR_MODEL=gpt-4o-mini
EXTRACTOR_MAX_TOKENS=1000
EXTRACTOR_TEMPERATURE=0.2

# ===========================================
# SESSION CONFIGURATION (Optional - Defaults shown)
# ===========================================
# How many chat messages to keep in context
CHAT_HISTORY_LIMIT=12

# Session expires after this many hours
SESSION_TTL_HOURS=24

# Extract data from session after this many minutes of inactivity
INACTIVITY_MINUTES=15
